1.环境安装
# 1.虚拟环境创建：（虚拟环境中的cuda版本以下面的安装的版本为准）
ln -s /home/jin/6t/learn/env/agent /home/jin/anaconda3/envs/agent
conda create -p /home/jin/6t/learn/env/agent python==3.12
conda install pytorch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1  pytorch-cuda=11.8 -c pytorch -c nvidia
conda install mkl=2023.1.0

# 查看torch环境gpu是否可用
python -c "import torch; print('CUDA available:', torch.cuda.is_available()); print('CUDA version:', torch.version.cuda); print('Device count:', torch.cuda.device_count())"

# 2.大模型调用通用格式依赖库安装
pip install openai
pip install anthropic
pip install langchain
pip install langchain-openai
pip install langchain-mcp-adapters
